const readfile = require('./src/readfile')
const sanitizeInt = require('./src/sanitizeInt')
const sanitizeFloat = require('./src/sanitizeFloat')

module.exports = {
    readfile,
    sanitizeInt,
    sanitizeFloat,
}
